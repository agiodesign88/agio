{
  "name": "agio-srm-map-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check public/app.mjs && node --check public/shell.mjs && node --check public/records.mjs",
    "build": "node tools/verify-deployment.mjs"
  },
  "engines": {
    "node": "22.x"
  }
}
